name: gym_managament
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path: ^1.9.0
  firebase_core: ^3.10.1
  cloud_firestore: ^5.6.1
  firebase_auth: ^5.5.0
  firebase_storage: ^12.3.7
  go_router: ^14.0.0
  file_picker: ^8.1.7
  provider: ^6.1.5+1
  intl: ^0.19.0
  uuid: ^4.4.0
  fl_chart: ^0.71.0
  device_info_plus: ^11.1.3
  csv: ^6.0.0
  share_plus: ^10.1.4
  path_provider: ^2.1.3 # Thêm để lưu file CSV tạm thời

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
